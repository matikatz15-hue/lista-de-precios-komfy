# PostgreSQL connection string (Neon, Supabase, Railway, etc.)
DATABASE_URL="postgresql://user:password@host:5432/komfy"

# NextAuth — generá un secreto con: openssl rand -base64 32
NEXTAUTH_SECRET="tu-secreto-aqui"
NEXTAUTH_URL="http://localhost:3000"

# Vercel Blob — habilitá el store en tu proyecto de Vercel y pegá el token
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxx"
